# Copy to .env.local and fill in once a Supabase project exists.
# Until then, src/lib/supabase/client.ts stays inert — the app runs entirely
# on local storage and never imports this file today.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
